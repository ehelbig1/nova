apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: forward-auth
  namespace: traefik
spec:
  forwardAuth:
    address: https://authentik
    trustForwardHeader: true
    tls:
      insecureSkipVerify: true